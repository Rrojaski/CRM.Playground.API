############################################################
# OS / Editor
############################################################
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# JetBrains / Rider
.idea/
*.sln.iml

############################################################
# Visual Studio / MSBuild / .NET
############################################################
## Build results
[Bb]in/
[Oo]bj/
[Ll]og/
*.log

## VS cache / user settings
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

## Resharper / other tooling
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user

## NUnit / test results
TestResults/
*.trx
*.coverage
*.coveragexml
coverage/
coverage-report/
.coverlet/
report/
*.opencover.xml

## ASP.NET Scaffolding
ScaffoldingReadMe.txt

############################################################
# NuGet
############################################################
# Optional: keep packages out of repo (recommended)
**/packages/
# But keep any custom .targets/.props that might live there
!**/packages/build/
!**/packages/build/**

# NuGet lock files (choose your preference)
# packages.lock.json

############################################################
# EF Core / LocalDB / SQLite / SQL artifacts
############################################################
# Local dev databases or artifacts if you ever use them
*.mdf
*.ldf
*.ndf
*.db
*.sqlite
*.sqlite3

# EF Core migrations snapshots are SOURCE CODE -> keep them tracked!
# (so intentionally NOT ignoring Migrations/)

############################################################
# Secrets / Environment / Certificates
############################################################
# .NET user secrets are not in repo, but keep config files safe:
appsettings.*.json
!appsettings.Development.json
!appsettings.json

# If you store environment settings for local dev, ignore them:
.env
.env.*
!.env.example

# Certificates
*.pfx
*.p12
*.pem
*.key

############################################################
# Docker / Containers
############################################################
# Local override files
docker-compose.override.yml
docker-compose.*.override.yml

# Docker build artifacts (rare)
**/.docker/

############################################################
# Kubernetes / Helm (local generated)
############################################################
# Helm dependencies / charts build output
charts/**/charts/
charts/**/tmp/
*.tgz

# If you use kustomize
**/kustomize/**/build/
**/kustomize/**/dist/

############################################################
# Node / Next.js / React Native / Expo
############################################################
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Next.js
.next/
out/
.vercel/

# Turborepo
.turbo/

# React Native / Expo
.expo/
.expo-shared/
*.mobileprovision
*.p8
*.keystore
*.jks

# Metro / watchman
.watchmanconfig
*.hmap

# iOS
ios/Pods/
ios/build/
*.xcworkspace
*.xcodeproj/project.xcworkspace/
*.xcuserstate
*.xcuserdata/

# Android
android/.gradle/
android/app/build/
android/build/
*.apk
*.aab

############################################################
# Logs / temp
############################################################
*.tmp
*.temp
*.swp
*.bak
*.old
*.orig
tmp/
temp/

############################################################
# Azure / DevOps / Terraform / Bicep local outputs
############################################################
# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Bicep / ARM outputs
*.bicepvars.json
azuredeploy.parameters.json
**/bin/Debug/**/publish/
**/bin/Release/**/publish/

############################################################
# Optional: Publish output
############################################################
publish/
artifacts/
dist/
``